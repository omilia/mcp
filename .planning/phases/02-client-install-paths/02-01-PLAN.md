---
phase: 02-client-install-paths
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/cli.test.js
autonomous: true
requirements: [CLI-05]
must_haves:
  truths:
    - "Every emitted config block (cursor/claude/claude-code/vscode/codex) uses npx -y github:omilia/mcp run"
    - "The full test suite (node --test) passes with zero failures"
  artifacts:
    - path: "test/cli.test.js"
      provides: "Updated Cursor-config fixture and PAT golden-SHA constant matching github:omilia/mcp output"
      contains: "github:omilia/mcp"
  key_links:
    - from: "test/cli.test.js"
      to: "lib/config.js PACKAGE_NAME"
      via: "asserted args array literal"
      pattern: "github:omilia/mcp"
---

<objective>
Fix the two stale fixtures in test/cli.test.js so the full suite is green and every assertion reflects the GitHub distribution spec (CLI-05). lib/config.js already emits `github:omilia/mcp`; the suite has two failures that assert the old `@omilia/mcp-server` string and a stale golden SHA.

Purpose: Establish a green baseline before the Claude Code install rewrite (Plan 02) lands. A green suite is the contract that Plans 02 and 03 build on.
Output: test/cli.test.js with corrected `args` literal and recomputed PAT_INIT_GOLDEN_SHA256.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
From lib/config.js — the source of truth the fixtures must match:
- `export const PACKAGE_NAME = "github:omilia/mcp"` — already correct; do NOT change config.js.
- `buildServerDefinition` emits `{ command: "npx", args: ["-y", PACKAGE_NAME, "run"], env }`.
- `buildCursorConfig()` returns `{ mcpServers: { OCP: { command:"npx", args:["-y","github:omilia/mcp","run"], env:{ OCP_BASE_URL:"your-ocp-base-url", OCP_ACCESS_TOKEN:"your-ocp-access-token" } } } }`.

Verified actual `init --client claude --print` SHA-256 (PAT default) = `9f03b6648fc1e5573acf9bc3fc5bc0c7f56ee3f839fe5bb8acf5d4d498103647`. The executor MUST recompute this itself rather than trusting this value (see Task 1 verify).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update stale args fixture and recompute PAT golden SHA</name>
  <files>test/cli.test.js</files>
  <read_first>
    - test/cli.test.js (lines 13-18 PAT_INIT_GOLDEN_SHA256 constant; lines 41-56 "builds Cursor config with inline literal placeholders by default"; lines 215-228 "PAT init for claude produces byte-identical output to golden SHA")
    - lib/config.js (PACKAGE_NAME, buildServerDefinition, buildClientConfig — confirm output shape)
  </read_first>
  <behavior>
    - Test "builds Cursor config with inline literal placeholders by default" passes: the asserted `args` array is `["-y", "github:omilia/mcp", "run"]`.
    - Test "PAT init for claude produces byte-identical output to golden SHA" passes: PAT_INIT_GOLDEN_SHA256 equals the SHA-256 of the actual current `init --client claude --print` stdout.
    - No other test changes behavior; all previously-passing tests still pass.
  </behavior>
  <action>
    In test/cli.test.js, change the `args` literal inside the "builds Cursor config with inline literal placeholders by default" assertion from `["-y", "@omilia/mcp-server", "run"]` to `["-y", "github:omilia/mcp", "run"]` (matches PACKAGE_NAME in lib/config.js per CLI-05).

    Recompute PAT_INIT_GOLDEN_SHA256: run `init --client claude --print` through runCli with a capturing io, hash the stdout with SHA-256, and set the constant (line ~18) to that hex digest. Do NOT hand-copy a value from this plan — derive it from actual output so the byte-identical contract is honest. Keep the surrounding comment block explaining the constant is a deliberate API-change gate.

    Do NOT modify lib/config.js — it already emits the correct spec. This task only reconciles the test fixtures to the existing output.
  </action>
  <verify>
    <automated>node --test test/cli.test.js 2>&1 | grep -E "# (pass|fail)"</automated>
  </verify>
  <done>test/cli.test.js asserts `github:omilia/mcp` in the Cursor args array; PAT_INIT_GOLDEN_SHA256 matches actual output; `node --test test/cli.test.js` reports `# fail 0`.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm full suite green and no residual @omilia/mcp-server references in tests</name>
  <files>test/cli.test.js</files>
  <read_first>
    - test/cli.test.js (full file — scan for any remaining `@omilia/mcp-server` occurrences)
    - test/wizard.test.js (no changes; just confirm it still passes as part of the full run)
  </read_first>
  <action>
    Grep the whole test directory for `@omilia/mcp-server`; if any assertion still references it, update it to `github:omilia/mcp` (the project is GitHub-distribution only per PROJECT.md Out of Scope). Then run the full suite. Resolve any remaining failure that is a stale-spec fixture; do NOT alter lib/ source to make tests pass — config.js is the source of truth.
  </action>
  <verify>
    <automated>grep -rn '@omilia/mcp-server' test/ | grep -v '^#' | wc -l | tr -d ' '; node --test 2>&1 | grep -E "# (pass|fail)"</automated>
  </verify>
  <done>Zero `@omilia/mcp-server` references remain in test/; `node --test` (full suite) reports `# fail 0`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → asserted spec | Fixture strings encode the distribution identity; a wrong package name could silently ship a config pointing at an unintended package source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | PACKAGE_NAME spec drift | mitigate | Golden-SHA test pins byte-identical PAT output; recomputed from actual emission so any future drift fails the gate. |
| T-02-02 | Spoofing | install package identity | accept | Package source is `github:omilia/mcp`; no token or secret is present in fixtures (placeholders only). |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; node stdlib + node:test only. No `claude mcp add` invocation here. |
</threat_model>

<verification>
- `node --test` full suite reports `# fail 0`.
- `grep -rn '@omilia/mcp-server' test/` returns nothing.
- The Cursor and claude PAT print paths emit `github:omilia/mcp` in their `args` arrays.
</verification>

<success_criteria>
- Both previously-failing tests pass.
- The entire suite is green (precondition for Plans 02 and 03).
- No lib/ source files changed (config.js already correct).
</success_criteria>

<output>
Create `.planning/phases/02-client-install-paths/02-01-SUMMARY.md` when done.
</output>
