---
phase: 02-client-install-paths
plan: "03"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - test/cli.test.js
  - manifest.json
autonomous: true
requirements: [CLI-04, CLI-05]
must_haves:
  truths:
    - "Claude Desktop (client claude) init writes config to the macOS path ~/Library/Application Support/Claude/claude_desktop_config.json"
    - "The written claude_desktop_config.json uses npx -y github:omilia/mcp run"
    - "claude is present in JSON_MCP_CLIENTS after 02-02 (asserted, not silently restored)"
    - "The .mcpb bundle manifest is internally consistent: server command, args, and env keys match the OCP env contract"
  artifacts:
    - path: "test/cli.test.js"
      provides: "End-to-end claude-desktop write-path test asserting correct location and github spec"
      contains: "claude_desktop_config.json"
    - path: "manifest.json"
      provides: "Bundle manifest with consistent command/args/env (verified, corrected if drifted)"
      contains: "OCP_BASE_URL"
  key_links:
    - from: "lib/config.js clientConfigPath('claude')"
      to: "~/Library/Application Support/Claude/claude_desktop_config.json"
      via: "platform path resolution"
      pattern: "claude_desktop_config.json"
    - from: "lib/cli.js writeClientConfig"
      to: "claude_desktop_config.json"
      via: "JSON mcpServers write with mode 0o600"
      pattern: "mcpServers"
---

<objective>
Verify the Claude Desktop install path end-to-end: confirm `init --client claude --write` lands JSON at the correct macOS location with `npx -y github:omilia/mcp run`, and verify the `.mcpb` bundle manifest is internally consistent with the OCP env contract (CLI-04). Confirm the GitHub spec is used consistently across the Claude Desktop path (CLI-05).

Purpose: CLI-04 (Claude Desktop verified end-to-end via JSON write and/or .mcpb bundle), and the cross-client consistency half of CLI-05.
Output: an end-to-end claude-desktop write test in test/cli.test.js, and a verified (corrected only if drifted) manifest.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
From lib/config.js:
- clientConfigPath("claude", env) returns ${HOME}/Library/Application Support/Claude/claude_desktop_config.json (macOS — keep as-is; CLI-04 says claude keeps the JSON write).
- buildClientConfig("claude", opts) returns { mcpServers: { OCP: { command:"npx", args:["-y","github:omilia/mcp","run"], env } } }.
- claude remains in JSON_MCP_CLIENTS after Plan 02 (only claude-code was removed). This plan ASSERTS that invariant rather than depending on it implicitly.

From lib/cli.js:
- writeClientConfig(client, config, options) writes JSON with rootKey "mcpServers" (for claude) and mode 0o600; conflict detection via --force.
- resolveWritePath(client, options) = options.path || clientConfigPath(client).

manifest.json (the .mcpb bundle descriptor) — current shape:
- server.mcp_config: { command:"node", args:["${__dirname}/bin/ocp-mcp.js","run"], env:{ OCP_BASE_URL:"${OCP_BASE_URL}", OCP_ACCESS_TOKEN:"${OCP_ACCESS_TOKEN}" } }.
- user_config: OCP_BASE_URL + OCP_ACCESS_TOKEN (sensitive).
- Note: the bundle is self-contained, so it correctly runs the bundled bin/ocp-mcp.js run via node (NOT npx) — this is the intended bundle behavior. The env keys (OCP_BASE_URL, OCP_ACCESS_TOKEN) must match the PAT env contract in getEnvBlock.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: End-to-end Claude Desktop write-path verification</name>
  <files>test/cli.test.js</files>
  <read_first>
    - lib/config.js (clientConfigPath claude branch lines 69-71; buildClientConfig; getEnvBlock; JSON_MCP_CLIENTS after Plan 02)
    - lib/cli.js (writeClientConfig, resolveWritePath, finishInit — confirm claude still routes through the JSON write path after Plan 02)
    - test/cli.test.js (existing claude/cursor write tests as patterns: lines 177-198 write-to-explicit-path + force-conflict)
  </read_first>
  <action>
    Add a test "claude remains a JSON client after Plan 02 (CLI-04 invariant)": import JSON_MCP_CLIENTS from "../lib/config.js" and ASSERT `JSON_MCP_CLIENTS.has("claude") === true`. Do NOT silently restore it. If the assertion fails, the test FAILS with a message pointing at Plan 02 (02-02) as the defect source — e.g. `assert.ok(JSON_MCP_CLIENTS.has("claude"), "claude must stay in JSON_MCP_CLIENTS; 02-02 removed it incorrectly — fix 02-02, do not patch here")`. This plan does NOT modify lib/config.js; it only asserts the invariant 02-02 was responsible for preserving.

    Add a test asserting the Claude Desktop platform location: clientConfigPath("claude", { HOME: "/home/user" }) equals /home/user/Library/Application Support/Claude/claude_desktop_config.json (CLI-04 correct location).

    Add an end-to-end write test: run runCli(["init","--client","claude","--write","--path", tmpfile, "--base-url","https://ocp.example.com","--access-token","pat-desktop-1234"], io); assert exit 0; read the written file; assert it parses as JSON with mcpServers.OCP.command === "npx" and mcpServers.OCP.args deep-equals ["-y","github:omilia/mcp","run"] (CLI-05 consistency) and the env carries the supplied base URL plus token. Use --path to a tmp file (the literal ~/Library/... path is not writable in CI; the path-resolution assertion above covers the real default location).

    If a default-path (no --path) assertion is desired, assert only the resolved path string via clientConfigPath, not an actual write, to avoid touching the real user directory.
  </action>
  <verify>
    <automated>node --test test/cli.test.js 2>&1 | grep -E "# (pass|fail)"</automated>
  </verify>
  <done>A test asserts JSON_MCP_CLIENTS.has("claude") and fails pointing at 02-02 if absent; clientConfigPath("claude") resolves to the macOS claude_desktop_config.json path; an end-to-end write test confirms the written file uses npx -y github:omilia/mcp run; full suite reports fail 0. No lib/ source modified by this plan.</done>
</task>

<task type="auto">
  <name>Task 2: Verify .mcpb bundle manifest consistency with the OCP env contract</name>
  <files>manifest.json, test/cli.test.js</files>
  <read_first>
    - manifest.json (full file — server.mcp_config command/args/env, user_config keys, server.entry_point)
    - lib/config.js (getEnvBlock PAT shape — the authoritative OCP_BASE_URL/OCP_ACCESS_TOKEN env contract)
  </read_first>
  <action>
    Verify manifest.json is internally consistent: server.entry_point matches the mcp_config.args script path (bin/ocp-mcp.js), the run subcommand is present ("run"), and the env keys (OCP_BASE_URL, OCP_ACCESS_TOKEN) exactly match the PAT env keys produced by getEnvBlock("pat", ...). The bundle correctly uses node ${__dirname}/bin/ocp-mcp.js run (self-contained bundle) — do NOT rewrite it to npx; that distinction is intentional for the bundle.

    If any drift exists (mismatched entry_point vs args path, missing run arg, env key not matching the PAT contract), correct manifest.json. If consistent, leave it unchanged.

    Add a lightweight test in test/cli.test.js that reads manifest.json (via readFileSync + JSON.parse from the repo root using a path relative to the test file), and asserts: manifest.server.mcp_config.args last element is "run" and a prior element contains bin/ocp-mcp.js; Object.keys(manifest.server.mcp_config.env) deep-equals Object.keys(getEnvBlock("pat", { useEnvVars: true })) (both equal OCP_BASE_URL, OCP_ACCESS_TOKEN). This locks bundle/env consistency for CLI-04 so future env-contract changes flag a manifest mismatch.
  </action>
  <verify>
    <automated>node --test test/cli.test.js 2>&1 | grep -E "# (pass|fail)"</automated>
  </verify>
  <done>manifest.json env keys match the PAT contract and args end with "run"; a test locks bundle/env consistency; full suite reports fail 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| init process → claude_desktop_config.json | Token written to a config file on disk; file permissions matter (0o600). |
| .mcpb bundle manifest → Claude Desktop runtime | env keys declared in the manifest are surfaced to the user as config fields; a wrong key would silently fail auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Information Disclosure | claude_desktop_config.json file perms | mitigate | writeClientConfig writes with mode 0o600 (existing); the end-to-end test exercises the real write path, preserving that mode. |
| T-02-08 | Tampering | manifest/env-contract drift | mitigate | Test asserts manifest env keys deep-equal the PAT getEnvBlock keys; drift fails the suite. |
| T-02-09 | Spoofing | wrong write location | mitigate | clientConfigPath("claude") asserted to the canonical macOS path; CLI-04 location verified. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; node:fs / node:test stdlib only. |
</threat_model>

<verification>
- node --test full suite reports fail 0.
- JSON_MCP_CLIENTS.has("claude") asserted true (fails pointing at 02-02 if removed).
- clientConfigPath("claude", {HOME:...}) returns the macOS claude_desktop_config.json path.
- End-to-end claude write produces a JSON file using npx -y github:omilia/mcp run.
- manifest.json env keys deep-equal the PAT getEnvBlock keys; args end with "run".
</verification>

<success_criteria>
- CLI-04: Claude Desktop write path verified end-to-end at the correct location; .mcpb manifest consistency locked by test.
- CLI-05: claude write path uses npx -y github:omilia/mcp run consistently.
</success_criteria>

<output>
Create `.planning/phases/02-client-install-paths/02-03-SUMMARY.md` when done.
</output>
