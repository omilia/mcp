---
phase: 02-client-install-paths
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - lib/install.js
  - lib/cli.js
  - lib/config.js
  - test/install.test.js
  - test/cli.test.js
autonomous: true
requirements: [CLI-01, CLI-02, CLI-03]
must_haves:
  truths:
    - "Completing init for claude-code invokes the claude CLI as `claude mcp add OCP --scope user --env KEY=value ... -- npx -y github:omilia/mcp run`"
    - "claude mcp add is invoked with an argument array (never a shell string); the token reaches the child only via the args array"
    - "No code path writes mcpServers to ~/.claude/settings.json for claude-code"
    - "When claude is not on PATH, init prints an installable config snippet plus actionable instructions and exits non-zero-free (graceful), not a silent failure"
    - "The printed CLI-absent fallback masks the secret value (does not echo the raw token)"
  artifacts:
    - path: "lib/install.js"
      provides: "claudeCodeInstall via injectable spawn + claudeAvailable detection + buildClaudeMcpAddArgs + CLI-absent fallback snippet"
      exports: ["buildClaudeMcpAddArgs", "claudeAvailable", "installClaudeCode"]
      min_lines: 40
    - path: "test/install.test.js"
      provides: "Tests asserting the captured argv array and the CLI-absent fallback (masked)"
      contains: "mcp add"
  key_links:
    - from: "lib/cli.js finishInit"
      to: "lib/install.js installClaudeCode"
      via: "claude-code branch dispatch"
      pattern: "installClaudeCode"
    - from: "lib/install.js"
      to: "node:child_process spawnSync"
      via: "injected spawn capturing argv"
      pattern: "spawn"
---

<objective>
Install Claude Code via the official `claude mcp add` CLI instead of writing `~/.claude/settings.json`. Add a dedicated install module with an injectable spawn so tests assert the exact argv array, remove the broken claude-code config-path/write branch, and degrade gracefully with a masked printable snippet when `claude` is not on PATH.

Purpose: CLI-01 (correct invocation), CLI-02 (kill the settings.json write path), CLI-03 (graceful CLI-absent fallback). This is the core of Phase 2.
Output: lib/install.js (new), rewired claude-code branch in lib/cli.js, removed claude-code path in lib/config.js, test/install.test.js (new), updated claude-code tests in test/cli.test.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/01-interactive-wizard/01-03-SUMMARY.md

<interfaces>
From lib/cli.js (Plan 01 baseline):
- `runCli(argv, io = process)` — io carries `stdout.write`, `stderr.write`, optional `input`.
- `finishInit(effectiveOptions, io)` — synchronous tail; currently for claude-code it falls through to `buildClientConfig` + print/write. effectiveOptions has: client, serverName, baseUrl, accessToken, username, password, realm, useEnvVars, authChoice, write, force, path, print.
- `writeClientConfig`, `resolveWritePath` — used by the JSON-write clients.

From lib/config.js:
- `getEnvBlock(authChoice, options)` → returns the env object: PAT shape `{OCP_BASE_URL, OCP_ACCESS_TOKEN}`, keycloak shape `{OCP_BASE_URL, OCP_USERNAME, OCP_PASSWORD, OCP_KEYCLOAK_REALM}`. Reuse this to derive the --env pairs (single source of truth for env keys).
- `PACKAGE_NAME = "github:omilia/mcp"`, `DEFAULT_SERVER_NAME = "OCP"`.
- `clientConfigPath(client, env)` — currently returns `~/.claude/settings.json` for claude-code (lines 73-75). This branch must be REMOVED (CLI-02).
- `JSON_MCP_CLIENTS = new Set(["cursor","claude","claude-code"])` — claude-code must be REMOVED from this set so the JSON write path no longer applies to it (CLI-02).

From lib/wizard.js:
- `maskSecret(value)` — reveals at most last 4 chars; reuse for the CLI-absent fallback so the printed snippet does not echo the raw token.

`claude mcp add` reference (research disabled — authoritative):
`claude mcp add <name> --scope user --env KEY=value [--env KEY=value ...] -- npx -y github:omilia/mcp run`
- `--scope user` default; `--env KEY=value` repeatable; `--` separates the server command from claude's flags.
- Invoke via `node:child_process` spawnSync with an ARGUMENT ARRAY; capture `status` (exit code). Never build a shell string.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create lib/install.js — argv builder, availability detection, install with injectable spawn, masked fallback</name>
  <files>lib/install.js, test/install.test.js</files>
  <read_first>
    - lib/config.js (getEnvBlock, PACKAGE_NAME, DEFAULT_SERVER_NAME — to build env pairs and the server command consistently)
    - lib/wizard.js (maskSecret — for the fallback)
    - lib/cli.js (finishInit, io shape — to match the interface the installer is called with)
    - .planning/codebase/CONVENTIONS.md (ESM, node: prefix, io injection, return integer exit codes, never throw from public API)
  </read_first>
  <behavior>
    - `buildClaudeMcpAddArgs({serverName, authChoice, baseUrl, accessToken, username, password, realm, useEnvVars})` returns an array beginning `["mcp","add","OCP","--scope","user"]`, followed by one `"--env"` + `"KEY=value"` pair per entry from getEnvBlock(authChoice, opts) (in object order), then `"--"`, `"npx"`, `"-y"`, `"github:omilia/mcp"`, `"run"`. Server name defaults to "OCP".
    - For PAT: argv contains `"--env"` then `"OCP_ACCESS_TOKEN=<token>"`; the token appears ONLY as an element of the array, never concatenated into a single shell string element.
    - For keycloak: argv contains OCP_USERNAME / OCP_PASSWORD / OCP_KEYCLOAK_REALM env pairs and NO OCP_ACCESS_TOKEN pair.
    - `claudeAvailable(spawn)` returns true when the injected spawn for `["--version"]` reports status 0, false when spawn throws ENOENT or returns non-zero/null status.
    - `installClaudeCode(options, io, { spawn })` when claude IS available: calls spawn with command `"claude"` and the buildClaudeMcpAddArgs array, returns 0 on status 0, returns 1 and writes the claude stderr-ish message to io.stderr on non-zero status. Returns the captured argv to the spawn so a test can assert it.
    - `installClaudeCode` when claude is NOT available (CLI-03): does NOT throw; writes to io.stdout an actionable message naming `claude mcp add` and a copy-pasteable snippet of the equivalent command, with the token rendered via maskSecret (raw token absent), and returns 0 (graceful — install guidance delivered, not a crash).
  </behavior>
  <action>
    Create lib/install.js (ESM, node: prefix). Import getEnvBlock, PACKAGE_NAME, DEFAULT_SERVER_NAME from "./config.js" and maskSecret from "./wizard.js". Default the spawn dependency to `spawnSync` from "node:child_process" but accept an injected `spawn` for tests (CLI-01 testability requirement).

    `buildClaudeMcpAddArgs(opts)`: derive env via getEnvBlock(opts.authChoice ?? "pat", opts); flatten each `[k,v]` to `"--env", \`${k}=${v}\`` (template-string element — value stays a single array element, never shell-interpolated). Compose `["mcp","add", opts.serverName ?? DEFAULT_SERVER_NAME, "--scope","user", ...envPairs, "--", "npx","-y", PACKAGE_NAME, "run"]`.

    `claudeAvailable(spawn = spawnSync)`: call `spawn("claude", ["--version"])` inside try/catch; return `result && result.status === 0`. Treat thrown ENOENT (spawn throw) as false.

    `installClaudeCode(options, io, deps = {})`: let `spawn = deps.spawn ?? spawnSync`. If `!claudeAvailable(spawn)` → write the CLI-absent fallback (see below) to io.stdout and return 0. Otherwise build argv via buildClaudeMcpAddArgs(options), call `spawn("claude", argv)`, on status 0 write a one-line success notice to io.stdout and return 0; on non-zero/null status write an error referencing the exit code to io.stderr and return 1. Never throw (CONVENTIONS: catch and return code).

    CLI-absent fallback text: explain claude CLI was not found on PATH, that Claude Code is installed via `claude mcp add`, give install guidance, and print the equivalent command with the env values rendered through maskSecret so the raw token never lands on stdout (CLI-03 / threat T-02-04). Document in a comment that masked output is intentional.

    Write test/install.test.js: inject a fake spawn that records `{cmd, args}`. Assert (a) PAT argv equals the expected array including `--scope user`, the masked-free token pair, the `--` separator, and `npx -y github:omilia/mcp run`; (b) keycloak argv has the three keycloak env pairs and no OCP_ACCESS_TOKEN; (c) claudeAvailable returns false when the fake spawn throws ENOENT; (d) installClaudeCode with an unavailable fake spawn returns 0, writes guidance to stdout, and the raw token string is ABSENT while a masked tail is present; (e) installClaudeCode with available spawn returning status 0 returns 0 and the recorded args deep-equal buildClaudeMcpAddArgs output.
  </action>
  <verify>
    <automated>node --test test/install.test.js 2>&1 | grep -E "# (pass|fail)"</automated>
  </verify>
  <done>lib/install.js exports buildClaudeMcpAddArgs, claudeAvailable, installClaudeCode; test/install.test.js passes with `# fail 0`; PAT argv asserted as array with token as a single element; CLI-absent path returns 0 with masked snippet and no raw token.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire claude-code through installClaudeCode; remove the settings.json write path</name>
  <files>lib/cli.js, lib/config.js, test/cli.test.js</files>
  <read_first>
    - lib/cli.js (finishInit lines 43-73; writeClientConfig, resolveWritePath)
    - lib/config.js (JSON_MCP_CLIENTS line 15, clientConfigPath claude-code branch lines 73-77)
    - test/cli.test.js (lines 200-213: "clientConfigPath resolves claude-code to ~/.claude/settings.json" and "writes claude-code config to an explicit path" — both assert the removed behavior and MUST be replaced)
    - lib/install.js (installClaudeCode signature from Task 1)
  </read_first>
  <behavior>
    - finishInit, when `effectiveOptions.client === "claude-code"`, dispatches to installClaudeCode (passing options + io) instead of buildClientConfig + JSON write/print; returns the installer's exit code.
    - No code path produces `~/.claude/settings.json` for claude-code: clientConfigPath("claude-code", ...) returns undefined and claude-code is not in JSON_MCP_CLIENTS.
    - A test injects a fake spawn into the claude-code init path and asserts the captured argv is the `claude mcp add ... -- npx -y github:omilia/mcp run` array (CLI-01).
    - The old "writes claude-code config to an explicit path" expectation is replaced; claude-code no longer writes settings.json.
  </behavior>
  <action>
    In lib/config.js: remove claude-code from JSON_MCP_CLIENTS (it becomes `new Set(["cursor","claude"])`); keep SUPPORTED_CLIENTS including claude-code so it remains a valid client. Remove the `if (client === "claude-code") return ~/.claude/settings.json` branch from clientConfigPath so it falls through to `return undefined` (CLI-02). Do not touch the env-block / PACKAGE_NAME logic.

    In lib/cli.js: import installClaudeCode from "./install.js". In finishInit, before the buildClientConfig call, add a branch: `if (effectiveOptions.client === "claude-code") return installClaudeCode(effectiveOptions, io)`. So claude-code is fully handled by the installer; the print/write JSON path is reserved for cursor/claude/vscode/codex. Keep finishInit synchronous (installClaudeCode is synchronous via spawnSync). To keep the installer testable per CLI-01, allow finishInit/installClaudeCode to receive an injected spawn through io or a deps param — pass `io.spawn` through to installClaudeCode if present (document this injection point so test/cli.test.js can supply a fake spawn).

    In test/cli.test.js: REPLACE the two stale claude-code tests. (a) "clientConfigPath resolves claude-code to ~/.claude/settings.json" → change to assert clientConfigPath("claude-code", {HOME:"/home/user"}) returns undefined (CLI-02). (b) "writes claude-code config to an explicit path" → replace with a test that runs `init --client claude-code --base-url URL --access-token TOK` with an io carrying a fake spawn, asserts exit 0, and asserts the recorded spawn args contain `["mcp","add",...,"--","npx","-y","github:omilia/mcp","run"]` (CLI-01). Ensure no file is written and `~/.claude/settings.json` is never referenced.
  </action>
  <verify>
    <automated>grep -n "settings.json" lib/config.js lib/cli.js | grep -v '^#'; node --test 2>&1 | grep -E "# (pass|fail)"</automated>
  </verify>
  <done>finishInit dispatches claude-code to installClaudeCode; JSON_MCP_CLIENTS excludes claude-code; clientConfigPath("claude-code") returns undefined; the two stale claude-code tests are replaced (path→undefined, write→spawn-argv assertion); full suite `# fail 0`; no `settings.json` reference remains for claude-code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| init process → claude CLI child | The access token / Keycloak password crosses into a spawned child process. Argv is visible in `ps`; shell history exposure occurs only if shell-interpolated. |
| init process → stdout (CLI-absent fallback) | The fallback snippet is printed to the terminal; raw secrets would otherwise be echoed and may be logged/scrolled. |
| lib/config.js clientConfigPath | Previously returned a path Claude Code does not read, silently dropping config in the wrong file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | claude mcp add token via shell history | mitigate | Invoke spawnSync with an argument ARRAY (never a shell string / `shell:true`); the token is a single array element, never parsed by a shell. Test asserts argv array shape. |
| T-02-04 | Information Disclosure | CLI-absent fallback printing raw token to stdout | mitigate | Fallback renders env values through maskSecret; test asserts raw token absent and masked tail present. |
| T-02-05 | Tampering | removed clientConfigPath claude-code branch | mitigate | clientConfigPath("claude-code") returns undefined and claude-code removed from JSON_MCP_CLIENTS so no settings.json is ever written; test asserts undefined. |
| T-02-06 | Denial of Service | claude CLI missing causing crash | mitigate | claudeAvailable wraps spawn in try/catch; ENOENT treated as unavailable; installClaudeCode returns 0 with guidance, never throws. |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; `node:child_process` is stdlib. No external package added. |
</threat_model>

<verification>
- `node --test` full suite `# fail 0`.
- Injected fake spawn records `claude` + the expected `mcp add ... -- npx -y github:omilia/mcp run` argv for both PAT and keycloak.
- `clientConfigPath("claude-code", {HOME:...})` returns undefined; `JSON_MCP_CLIENTS` excludes claude-code.
- CLI-absent path: returns 0, prints masked guidance, raw token absent from stdout.
- No `settings.json` reference for claude-code anywhere in lib/.
</verification>

<success_criteria>
- CLI-01: claude-code init invokes `claude mcp add` with correct name, `npx -y github:omilia/mcp run` command, and --env pairs, via an array (test-asserted).
- CLI-02: settings.json write path removed; claude-code no longer JSON-written.
- CLI-03: claude-absent prints a masked installable snippet + instructions, returns gracefully.
</success_criteria>

<output>
Create `.planning/phases/02-client-install-paths/02-02-SUMMARY.md` when done.
</output>
