---
phase: 04-docs-and-tests
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/cli.test.js
  - docs/test-coverage-map.md
autonomous: true
requirements: [TST-01, TST-02, TST-03, TST-04]
must_haves:
  truths:
    - "Each of TST-01..04 maps to one or more concrete, passing existing test names"
    - "The wizard prompt flow (client/auth/credentials) is covered by a passing test"
    - "The `claude mcp add` invocation AND the CLI-absent fallback are each covered by a passing test"
    - "Config writes to the correct location for each supported client (Claude Desktop write, Cursor write, claude-code via claude mcp add) are covered"
    - "Fully-flagged non-interactive invocations are covered and remain backward-compatible (golden SHA + no-prompt)"
    - "The full suite stays green (>=155 tests, 0 failures)"
  artifacts:
    - path: "docs/test-coverage-map.md"
      provides: "TST-01..04 -> concrete test name traceability table"
      contains: "TST-01"
    - path: "test/cli.test.js"
      provides: "Any genuine gap-fill tests; existing coverage preserved"
      contains: "test("
  key_links:
    - from: "docs/test-coverage-map.md"
      to: "test/cli.test.js, test/wizard.test.js, test/install.test.js"
      via: "named test references that exist and pass"
      pattern: "TST-0"
---

<objective>
Audit existing test coverage against TST-01..04, produce a traceability coverage
map, and add ONLY genuinely missing assertions. Phases 1-3 already wrote
extensive tests (wizard.test.js prompt flow, install.test.js + cli.test.js for
`claude mcp add` + fallback, cli.test.js for per-client writes and fully-flagged
golden-SHA invocations). This plan is an honest coverage audit + small gap-fill,
NOT a rewrite.

Purpose: Lock in the TST requirements by proving (not re-implementing) that each
is satisfied, and close any real gap surfaced by the audit.
Output: docs/test-coverage-map.md and any necessary additions to test/cli.test.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing coverage already in the repo (verified by grep of test names). The audit must
     reference these by exact test name. Add a new test ONLY if an audit row has no match. -->
<!-- TST-01 wizard prompt flow:
     - test/wizard.test.js "runWizard: bare PAT path — collects client, auth, base-url, token"
     - test/wizard.test.js "runWizard: keycloak path — collects username, password, realm uses default"
     - test/wizard.test.js "runWizard: PAT path — accessToken not echoed in captured output"
     - test/cli.test.js "interactive bare init reaches masked confirmation summary (WIZ-01, WIZ-04)"
   TST-02 claude mcp add + CLI-absent fallback:
     - test/cli.test.js "init --client claude-code invokes claude mcp add with correct argv (CLI-01)"
     - test/cli.test.js "claude-code --print emits the masked mcp add command, no spawn (CLI-01)"
     - test/install.test.js "installClaudeCode with available claude: records argv and returns 0"
     - test/install.test.js "installClaudeCode CLI-absent path: returns 0 (graceful)" / "writes guidance to stdout"
   TST-03 per-client config write location:
     - test/cli.test.js "init --client claude --write writes valid mcpServers JSON with npx github:omilia/mcp run (CLI-04, CLI-05)"
     - test/cli.test.js "clientConfigPath('claude') resolves to macOS claude_desktop_config.json path (CLI-04 location)"
     - test/cli.test.js "writes Cursor config to an explicit path"
     - test/cli.test.js "clientConfigPath returns undefined for claude-code (CLI-02: no settings.json write path)"
   TST-04 fully-flagged non-interactive backward compat:
     - test/cli.test.js "fully-flagged --print returns config JSON only, no prompts (WIZ-05)"
     - test/cli.test.js "PAT init for claude produces byte-identical output to golden SHA"
     - test/wizard.test.js "runWizard: fully-flagged — prompts for nothing, returns options unchanged" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author TST coverage map from existing tests (TST-01..04)</name>
  <files>docs/test-coverage-map.md</files>
  <read_first>
    - test/cli.test.js (all test names)
    - test/wizard.test.js (all test names)
    - test/install.test.js (all test names)
    - .planning/REQUIREMENTS.md (TST-01..04 definitions)
  </read_first>
  <action>
    Create docs/test-coverage-map.md with a heading "# Install-flow test coverage map" and a table with columns: Requirement, What it requires, Covering test(s) (file::name), Status. Add one row per TST-01..04, listing the exact existing test names that satisfy each (use the names enumerated in the <interfaces> block; re-verify each name exists by grepping the test files before recording it). For each requirement, mark Status = Covered only when at least one named test exists; if a row has no existing test, mark it Gap and name the specific missing assertion (this drives Task 2). Add a final note stating the suite size (run `node --test 2>&1 | tail -5` and record the pass count) and that all referenced tests are part of the green suite. Do not invent test names — every referenced name must be present in a test file.
  </action>
  <verify>
    <automated>grep -q 'TST-01' docs/test-coverage-map.md && grep -q 'TST-04' docs/test-coverage-map.md && for n in "runWizard: bare PAT path" "init --client claude-code invokes claude mcp add" "installClaudeCode CLI-absent path: returns 0" "fully-flagged --print returns config JSON only"; do grep -q "$n" docs/test-coverage-map.md || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `docs/test-coverage-map.md` contains rows for TST-01, TST-02, TST-03, TST-04
    - Each referenced test name exists in test/cli.test.js, test/wizard.test.js, or test/install.test.js (no fabricated names)
    - The map distinguishes Covered vs Gap and names any missing assertion
    - The recorded suite pass count matches the actual `node --test` output
  </acceptance_criteria>
  <done>An honest, verifiable traceability map from TST-01..04 to existing passing tests, with any genuine gaps explicitly named.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close any audit gap with a focused test (TST-01..04)</name>
  <files>test/cli.test.js</files>
  <read_first>
    - docs/test-coverage-map.md (the Gap rows from Task 1)
    - test/cli.test.js (existing createScriptedIo helper and test patterns)
    - lib/cli.js (runInit branches: wizard / no-TTY / synchronous)
  </read_first>
  <behavior>
    - If Task 1 found NO gaps, add exactly one consolidating regression test that asserts a full keycloak-auth wizard prompt flow drives a non-JSON outcome OR explicitly document "no gap" — pick whichever the audit dictates. Prefer the smallest addition that turns an audit Gap row into Covered.
    - Any added test: input = scripted io / flags per existing createScriptedIo pattern; expected output = exit code and a stdout/stderr assertion; planted secret must be absent from stdout/stderr.
    - Do NOT duplicate an assertion already covered by a named existing test.
  </behavior>
  <action>
    For each Gap row in docs/test-coverage-map.md, add one focused test to test/cli.test.js using the existing createScriptedIo helper and io-injection patterns (do not introduce a new test framework or npm dep — Node `node:test` only). Each new test name must reference the TST id it closes. After adding, update docs/test-coverage-map.md to flip that row from Gap to Covered with the new test name. If the audit found NO gaps (the expected outcome given Phases 1-3 coverage), add nothing to test/cli.test.js beyond what is needed and instead record in the map that TST-01..04 are fully satisfied by pre-existing tests — in that case this task is a no-op on test/cli.test.js and the verify below still holds (suite green). Never weaken or delete an existing test.
  </action>
  <verify>
    <automated>node --test 2>&1 | tail -5 | grep -qE 'fail 0' && test "$(node --test 2>&1 | grep -E '^# pass ' | awk '{print $3}')" -ge 155</automated>
  </verify>
  <acceptance_criteria>
    - `node --test` reports `# fail 0`
    - `# pass` count is >= 155 (no regressions; equal if no gap-fill was needed)
    - Every Gap row from Task 1 is now Covered (or the map records that no gaps existed)
    - No existing test was deleted or weakened; no new npm dependency added
  </acceptance_criteria>
  <done>The full suite is green and every TST-01..04 audit row is Covered, with at most minimal, non-duplicating additions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures -> assertions | Planted secrets in scripted input must never surface in captured stdout/stderr |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure | new gap-fill tests | mitigate | Any added test asserts the planted token is absent from captured stdout/stderr (mirrors existing no-leak assertions in cli/install/verify tests) |
| T-04-05 | Tampering | test suite integrity | mitigate | Existing tests are never deleted or weakened; gap-fill is additive only; verify gate requires `# fail 0` and pass count >= 155 |
| T-04-SC | Tampering | npm/pip installs | accept | No package installs — uses Node `node:test` only |
</threat_model>

<verification>
- `node --test` → `# fail 0`, `# pass` >= 155
- `docs/test-coverage-map.md` references only test names that exist in the suite
- Each TST-01..04 row resolves to a passing test (Covered) — no fabricated names
</verification>

<success_criteria>
- TST-01: wizard prompt flow (client/auth/credentials) mapped to passing test(s)
- TST-02: `claude mcp add` invocation + CLI-absent fallback mapped to passing test(s)
- TST-03: per-client config write location mapped to passing test(s)
- TST-04: fully-flagged non-interactive backward-compat mapped to passing test(s)
- Any genuine gap closed with a minimal, non-duplicating test; suite green (>=155, 0 fail)
</success_criteria>

<output>
Create `.planning/phases/04-docs-and-tests/04-02-SUMMARY.md` when done
</output>
