---
phase: 04-docs-and-tests
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/installation.md
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03]
must_haves:
  truths:
    - "No install example anywhere in README.md or docs/ references @omilia/mcp-server"
    - "docs/installation.md documents `claude mcp add` as the Claude Code install mechanism"
    - "docs no longer imply Claude Code config is written to ~/.claude/settings.json"
    - "A reader can find a section describing the interactive wizard (bare `init` with no flags) and what it prompts for"
    - "The wizard section mentions prereq checks (node>=20, uv) and the PASS/FAIL verification summary"
  artifacts:
    - path: "docs/installation.md"
      provides: "Claude Code claude-mcp-add install path + interactive wizard section"
      contains: "claude mcp add"
    - path: "README.md"
      provides: "Install section consistent with github:omilia/mcp and wizard-first entry"
      contains: "github:omilia/mcp"
  key_links:
    - from: "docs/installation.md"
      to: "lib/install.js (claude mcp add argv)"
      via: "documented command matches buildClaudeMcpAddArgs output"
      pattern: "claude mcp add"
    - from: "docs/installation.md"
      to: "lib/wizard.js / lib/verify.js"
      via: "wizard section describes prompt flow + PASS/FAIL summary"
      pattern: "wizard"
---

<objective>
Reconcile the in-repo developer docs (README.md, docs/installation.md) with the
behavior shipped in Phases 1-3: the GitHub distribution spec, the `claude mcp add`
Claude Code install path, and the new interactive wizard with prereq + smoke-test
verification.

Purpose: Docs are the developer's first contact with the install flow. They
currently document a flag-only `init` and imply Claude Code config is written to a
file Claude Code never reads. Closing DOC-01..03 makes the guide match reality.
Output: Updated README.md and docs/installation.md. No code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Documented commands MUST match the actual argv the tool emits. -->
<!-- From lib/install.js (claude mcp add path, CLI-01):
     claude mcp add OCP --scope user --env OCP_BASE_URL=... --env OCP_ACCESS_TOKEN=... -- npx -y github:omilia/mcp run
   When `claude` is absent (CLI-03): the tool prints a masked config snippet + guidance instead of failing.
   From lib/config.js: clientConfigPath('claude-code') now returns undefined (no ~/.claude/settings.json write).
   From lib/wizard.js: bare `init` prompts in order: client -> auth method -> base URL -> credentials -> masked confirmation summary.
   From lib/verify.js: after install, runVerification checks node>=20 + uv (preflight), runs a smoke test, prints a per-check [check]/[x] PASS/FAIL summary; --no-verify-install opts out; no credentials in output. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add claude mcp add path and assert GitHub spec (DOC-01, DOC-02)</name>
  <files>docs/installation.md, README.md</files>
  <read_first>
    - docs/installation.md (entire file — sections 1, 3, and the config-file table at line ~164)
    - README.md (lines 20-35 install section)
    - lib/install.js (buildClaudeMcpAddArgs, installClaudeCode — the exact argv and CLI-absent behavior)
    - lib/config.js (clientConfigPath claude-code returns undefined; JSON_MCP_CLIENTS excludes claude-code)
  </read_first>
  <action>
    In docs/installation.md, add a Claude Code subsection under "1. `npx` CLI installer" titled exactly "### Claude Code (`claude mcp add`)". State that for the `claude-code` client the CLI does NOT write a JSON config file — it invokes the official `claude mcp add` CLI, which registers the server where Claude Code actually reads MCP servers. Show the resolved command literally: `claude mcp add OCP --scope user --env OCP_BASE_URL=... --env OCP_ACCESS_TOKEN=... -- npx -y github:omilia/mcp run`. State the absent-CLI fallback: if `claude` is not on PATH, the tool prints a config snippet plus instructions and exits 0 (does not fail silently) — install the Claude Code CLI then re-run.
    In the config-file table (the row that currently maps "Claude Code | `~/.claude.json` ..."), change the Claude Code row to state that `init --client claude-code` uses `claude mcp add` (no JSON file is written by the installer); the `~/.claude.json` location is what `claude mcp add` manages on the user's behalf. Ensure no line implies the installer writes `~/.claude/settings.json`.
    Verify the GitHub spec (DOC-01): confirm by grep that neither README.md nor docs/ contains `@omilia/mcp-server`. They currently do not — this task keeps them clean; do not introduce the npm name anywhere.
    Do NOT add fenced credential-logging instructions. Where a token appears in an example command, use the placeholder form (e.g. `$OCP_ACCESS_TOKEN` or `your-ocp-access-token`), never a literal secret, and note that the wizard masks the token in its confirmation summary and config files keep mode 0600.
  </action>
  <verify>
    <automated>test "$(grep -rc '@omilia/mcp-server' README.md docs/ | grep -v ':0$' | wc -l | tr -d ' ')" = "0" && grep -q 'claude mcp add' docs/installation.md && grep -q 'github:omilia/mcp' README.md && test "$(grep -c 'settings.json' docs/installation.md)" -le "1"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc '@omilia/mcp-server' README.md docs/` reports 0 in every file (DOC-01)
    - `docs/installation.md` contains the literal string `claude mcp add` (DOC-02)
    - `docs/installation.md` contains the heading `### Claude Code (\`claude mcp add\`)`
    - No line in README.md or docs/installation.md states the installer writes `~/.claude/settings.json`
    - The documented argv matches lib/install.js: ends with `-- npx -y github:omilia/mcp run`
  </acceptance_criteria>
  <done>Docs use the GitHub spec exclusively, document `claude mcp add` as the Claude Code install path, and no longer imply a settings.json write.</done>
</task>

<task type="auto">
  <name>Task 2: Add interactive wizard section (DOC-03)</name>
  <files>docs/installation.md, README.md</files>
  <read_first>
    - docs/installation.md (top install-method table at line ~6; section 1 just edited in Task 1)
    - README.md (lines 20-29 install block)
    - lib/wizard.js (runWizard prompt order, PROMPT_FIELDS, buildConfirmationSummary masking)
    - lib/verify.js (runVerification, buildVerifySummary PASS/FAIL line)
    - lib/preflight.js (checkPrerequisites: node>=20, uv)
  </read_first>
  <action>
    In docs/installation.md, add a new top-level section titled exactly "## 0. Interactive wizard (recommended)" placed BEFORE "## 1. `npx` CLI installer". Describe that running `npx github:omilia/mcp init` with no flags launches a guided wizard. List the prompt order as it appears in lib/wizard.js: (1) select a client (Claude Code, Claude Desktop), (2) select auth method (PAT or Keycloak), (3) enter base URL, (4) enter credentials (token for PAT; username/password/realm for Keycloak), (5) review a confirmation summary with the secret masked before anything is written or run. State that the token is never echoed while typing and is masked in the summary. State that after confirmation the wizard performs prereq checks (node 20+ and `uv` on PATH, with install hints if missing), installs the config (via `claude mcp add` for Claude Code or a config write for Claude Desktop), runs a smoke test, and prints a PASS/FAIL verification summary before exiting. Mention `--no-verify-install` to skip the smoke test and that supplying all flags runs non-interactively (CI-safe).
    In the install-method comparison table at the top of installation.md, add a row for the interactive wizard as the first/recommended option (best for: first-time setup; friction: answer a few prompts; updates: re-run).
    In README.md, update the "## Install" section so the FIRST shown command is the bare wizard invocation `npx github:omilia/mcp init` (recommended), and keep the existing flag-driven one-liner as the non-interactive/CI alternative below it. Add one sentence pointing to the wizard section in docs/installation.md.
    Do not instruct users to paste raw tokens into committed/shared files; reference the masked confirmation and 0600 config handling.
  </action>
  <verify>
    <automated>grep -q '## 0. Interactive wizard' docs/installation.md && grep -qi 'confirmation summary' docs/installation.md && grep -qiE 'node 20\+|node >= ?20|node 20 or' docs/installation.md && grep -qiE 'PASS/FAIL|pass.{0,3}fail' docs/installation.md && grep -q 'npx github:omilia/mcp init' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/installation.md` contains the heading `## 0. Interactive wizard (recommended)` (DOC-03)
    - The wizard section lists the prompt order: client, auth method, base URL, credentials, confirmation
    - The wizard section mentions masked secret confirmation, node 20+ and uv prereq checks, and a PASS/FAIL summary
    - `README.md` shows `npx github:omilia/mcp init` (bare wizard) as the recommended first command
    - No instruction to log or commit raw credentials appears in the new content
  </acceptance_criteria>
  <done>Docs describe the interactive wizard flow end-to-end (prompts -> masked confirmation -> prereq + smoke-test verification -> PASS/FAIL), and README leads with the wizard invocation.</done>
</task>

<task type="auto">
  <name>Task 3: Flag Confluence developer guide for manual sync</name>
  <files>docs/installation.md</files>
  <read_first>
    - docs/installation.md (top of file)
  </read_first>
  <action>
    The "OCP MCP Server — Developer Guide" lives in Confluence and is maintained outside this repo — it is the internal source of truth and MUST NOT be edited by this tooling. Add a short HTML comment at the very top of docs/installation.md (a comment, not rendered content) noting that the internal Confluence "OCP MCP Server — Developer Guide" still documents the old `@omilia/mcp-server` name, the `~/.claude/settings.json` Claude Code path, and a flag-only `init`, and therefore needs a manual sync to match this guide (GitHub spec, `claude mcp add`, interactive wizard). State that the Confluence sync is out of scope for code execution and tracked as a manual follow-up. Do not add any Confluence URL, credentials, or internal links.
  </action>
  <verify>
    <automated>grep -qi 'Confluence' docs/installation.md && grep -q '<!--' docs/installation.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/installation.md` contains an HTML comment referencing the Confluence Developer Guide manual sync
    - The note states the Confluence sync is a manual, out-of-scope follow-up
    - No Confluence URL or internal credential appears
  </acceptance_criteria>
  <done>A reviewer reading docs/installation.md sees a recorded reminder that the internal Confluence guide needs manual reconciliation, without this tooling touching Confluence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> developer's shell | Example commands are copy-pasted and executed verbatim |
| docs -> committed config files | Examples may be pasted into files under version control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | install examples in docs | mitigate | All token examples use placeholders (`$OCP_ACCESS_TOKEN` / `your-ocp-access-token`), never literal secrets; wizard section states the token is masked in the confirmation summary and config files keep mode 0600 |
| T-04-02 | Information Disclosure | wizard section guidance | mitigate | Docs explicitly note credentials are never echoed/logged; no instruction to paste raw tokens into shared or committed files |
| T-04-03 | Tampering | documented `claude mcp add` command | accept | Command is reproduced from lib/install.js argv; no shell-interpolation pattern documented (token shown as placeholder, single value) |
| T-04-SC | Tampering | npm/pip installs | accept | No package installs in this plan — Markdown edits only |
</threat_model>

<verification>
- `node --test` still green (no code touched; suite unaffected)
- `grep -rc '@omilia/mcp-server' README.md docs/` → 0 in all files
- `grep -q 'claude mcp add' docs/installation.md` → match
- `grep -q '## 0. Interactive wizard' docs/installation.md` → match
- `grep -q 'npx github:omilia/mcp init' README.md` → match
</verification>

<success_criteria>
- DOC-01: No `@omilia/mcp-server` in README.md or docs/ (asserted by grep)
- DOC-02: `claude mcp add` documented as the Claude Code install path; no settings.json-write implication
- DOC-03: Interactive wizard section present, covering prompts, masked confirmation, prereq + smoke-test PASS/FAIL
- Confluence guide flagged for manual sync (out of scope for execution)
</success_criteria>

<output>
Create `.planning/phases/04-docs-and-tests/04-01-SUMMARY.md` when done
</output>
