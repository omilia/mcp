---
phase: 01-interactive-wizard
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - lib/cli.js
  - bin/ocp-mcp.js
  - test/cli.test.js
autonomous: true
requirements: [WIZ-01, WIZ-04, WIZ-05, WIZ-06]
must_haves:
  truths:
    - "Running init with no flags in a TTY launches the wizard instead of erroring"
    - "Running init with all required flags runs non-interactively with zero prompts (CI-safe)"
    - "A confirmation summary with masked secrets is shown before any change"
    - "No-TTY with insufficient flags exits with a clear actionable message, exit code 1, without hanging"
    - "Existing --print / --write flag behavior and the PAT golden-SHA contract remain intact"
  artifacts:
    - path: "lib/cli.js"
      provides: "runInit rewired: gate on needsPrompting; run wizard; show confirmation; preserve flag paths"
      contains: "needsPrompting"
    - path: "bin/ocp-mcp.js"
      provides: "Awaits a possibly-async runCli result"
      contains: "await"
    - path: "test/cli.test.js"
      provides: "Integration tests for wizard launch, no-TTY guard, fully-flagged non-interactive path, wizard+--write ordering, golden-SHA preservation"
  key_links:
    - from: "lib/cli.js"
      to: "lib/wizard.js"
      via: "runInit imports needsPrompting, runWizard, buildConfirmationSummary, missingPromptFields, buildNoTtyError"
      pattern: "from \"./wizard.js\""
    - from: "bin/ocp-mcp.js"
      to: "lib/cli.js"
      via: "await Promise.resolve(runCli(...)) for exit code"
      pattern: "runCli"
---

<objective>
Wire the wizard into the `init` command. Rewrite `runInit` in `lib/cli.js` so bare
`init` (in a TTY, missing required fields) runs `runWizard`, then shows a
masked confirmation summary (WIZ-04) before proceeding to the existing
print/write path. Fully-flagged invocations skip the wizard entirely (WIZ-05,
CI-safe). No-TTY with insufficient flags prints `buildNoTtyError` to stderr and
returns exit code 1 without hanging (WIZ-06). Update `bin/ocp-mcp.js` to await a
possibly-async `runCli`. Existing `--print`/`--write` behavior and the PAT
golden-SHA contract must remain byte-identical.

Purpose: This is the user-visible deliverable — bare `init` no longer errors
(WIZ-01). Phase 2 plugs install execution in after the confirmation step.
Output: rewired `lib/cli.js`, async-safe `bin/ocp-mcp.js`, extended `test/cli.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/01-interactive-wizard/01-01-PLAN.md
@.planning/phases/01-interactive-wizard/01-02-PLAN.md

<interfaces>
<!-- Contracts the executor wires together. -->

From lib/wizard.js (Plans 01-01 / 01-02):
- needsPrompting(options, { isTty }): boolean
- missingPromptFields(options): string[]
- async runWizard(options, io): completed options
- buildConfirmationSummary(options): string  (secrets masked)
- buildNoTtyError(missingFields): string

From lib/cli.js (current — DO NOT BREAK):
- export function runCli(argv, io = process)  → returns integer exit code today
- function runInit(args, io)  → parses options, builds config, print/write paths
- parseInitOptions(args) throws on bad flags with error.exitCode
- Currently parseInitOptions ALSO throws "Missing required option: --client" when
  options.client is undefined (lib/cli.js ~line 150). THIS guard is the Phase 1
  defect: it makes bare `init` error before any TTY/wizard branch. It must be
  removed from parseInitOptions and re-homed in runInit (see Task 1 action).
- runInit already has a post-parse guard: `if (!SUPPORTED_CLIENTS.has(options.client))`
  → writes "Unsupported client" to stderr and returns 1. This guard stays and
  catches a fully-flagged-but-bad/absent client in the non-interactive path.

From bin/ocp-mcp.js (current):
- process.exitCode = runCli(process.argv.slice(2));   // synchronous today

Backward-compat constraint: existing tests call `runCli([...flags...], io)` and
assert.equal on the integer return. Fully-flagged / --print / --write paths MUST
keep returning a plain integer synchronously. Only the interactive (prompting)
path may return a Promise<integer>. bin awaits both via Promise.resolve.

TTY detection: use io.input?.isTTY (default process.stdin.isTTY). Tests inject
io with input.isTTY to simulate TTY / no-TTY without a real terminal.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire runInit with wizard gate, confirmation, and no-TTY guard</name>
  <files>lib/cli.js, bin/ocp-mcp.js</files>
  <read_first>
    - lib/cli.js (the file being modified — runInit, parseInitOptions, runCli)
    - bin/ocp-mcp.js (the file being modified)
    - lib/wizard.js (needsPrompting, runWizard, buildConfirmationSummary, missingPromptFields, buildNoTtyError)
    - lib/config.js (SUPPORTED_CLIENTS, buildClientConfig — unchanged)
  </read_first>
  <behavior>
    - parseInitOptions no longer throws on a missing client; it returns options with client possibly undefined. Client presence is enforced LATER, in runInit, after the TTY/wizard branch.
    - When parseInitOptions succeeds and the resolved options are fully sufficient (needsPrompting false because all required flags present), runInit behaves EXACTLY as today: builds config and runs the print or write path, returning an integer synchronously. Golden-SHA test stays green.
    - When required fields are missing AND io.input.isTTY is true: runInit awaits runWizard(options, io) to fill the gaps, writes buildConfirmationSummary(merged) to io.stdout, then proceeds to the existing print/write path (default remains print). Returns a Promise<integer>.
    - When required fields are missing AND io.input.isTTY is falsy: runInit writes buildNoTtyError(missingPromptFields(options)) to io.stderr and returns 1, without invoking any prompt (no hang). (WIZ-06)
    - A fully-flagged-but-missing-client invocation (every other required field present so needsPrompting is false, yet client absent) falls through to the existing `SUPPORTED_CLIENTS.has(options.client)` guard, which already writes the error to stderr and returns 1.
    - The confirmation summary is written BEFORE any write/spawn occurs (WIZ-04). No credential appears unmasked in the summary.
    - runCli stays the dispatcher; for init it returns whatever runInit returns (integer or Promise<integer>). The run command path is unchanged.
    - parseInitOptions must still throw on invalid flags with the same messages/exit codes; --auth oauth still exits 2.
  </behavior>
  <action>
    In lib/cli.js, REMOVE the `if (!options.client) { throw new Error("Missing required option: --client"); }` guard from parseInitOptions (currently ~lines 150-152). parseInitOptions must return options with client possibly undefined; all OTHER throws (invalid --auth exit 2, --use-env-vars conflict, unsupported option, missing value) stay exactly as-is. Re-home client-presence enforcement into runInit AFTER the TTY/wizard branch.

    Import needsPrompting, missingPromptFields, runWizard, buildConfirmationSummary, buildNoTtyError from "./wizard.js". Restructure runInit so that AFTER parseInitOptions succeeds it branches in this order:
    (1) Compute `const promptNeeded = needsPrompting(options, { isTty: !!io.input?.isTTY });` and `const missing = missingPromptFields(options);`.
    (2) If `missing.length > 0` and `io.input?.isTTY` is falsy → write `buildNoTtyError(missing)` to io.stderr and return 1 (no prompt, no hang — WIZ-06). This is the no-TTY-without-client case.
    (3) If `missing.length > 0` and `io.input?.isTTY` is truthy → make this an async path: `const merged = await runWizard(options, io);` then continue with `merged` as the effective options (the wizard supplies client and other gaps in TTY mode). Write `buildConfirmationSummary(merged)` to io.stdout BEFORE any build/write step.
    (4) For the non-prompting path (fully-flagged), the effective options are `options` unchanged — synchronous, integer return, golden SHA preserved.

    For BOTH the wizard and non-wizard paths, after the effective options are resolved, keep the EXISTING `if (!SUPPORTED_CLIENTS.has(options.client)) { ... return 1; }` guard (operating on the effective options). In the fully-flagged-but-missing-client case (needsPrompting false, client undefined), control reaches this guard and returns 1 — that is the intended replacement for the deleted parseInitOptions throw. Then run the unchanged buildClientConfig + print/write logic and return its integer.

    Keep the bare-default behavior as --print (do NOT auto-write). Preserve writeClientConfig, resolveWritePath, helpText unchanged. To avoid duplicating the build+print/write block across the sync and async branches, extract it into a small synchronous helper (e.g. `finishInit(effectiveOptions, io)`) returning the integer; the async TTY branch awaits the wizard, writes the summary, then calls finishInit; the sync branch calls finishInit directly. runCli returns whatever runInit returns. In bin/ocp-mcp.js, change the entry to `process.exitCode = await Promise.resolve(runCli(process.argv.slice(2)))` using top-level await (the file is ESM).
  </action>
  <verify>
    <automated>node --test test/cli.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/cli.test.js`: the PAT golden-SHA test ("PAT init for claude produces byte-identical output to golden SHA") and all currently-passing tests still pass. (See note below re: the pre-existing `@omilia/mcp-server` Cursor assertion.)
    - Source assertion: `grep -c 'Missing required option: --client' lib/cli.js` returns 0 (the unconditional throw is removed from parseInitOptions).
    - Behavior: with io.input.isTTY=false and args=["init"] (no --client), runInit returns 1 and io.stderr contains "--client" (via buildNoTtyError) — NOT the string "Missing required option: --client".
    - Behavior: bare `init` over a TTY-emulating injected io with scripted answers (client, auth=pat, base-url, token) does NOT print "Missing required option: --client" and reaches a confirmation summary.
    - Source assertion: `grep -c 'await' bin/ocp-mcp.js` returns >=1.
    - Source assertion: `grep -c 'from "./wizard.js"' lib/cli.js` returns 1.

    Note (known pre-existing issue, out of Phase 1 scope): the pre-existing test "builds Cursor config with inline literal placeholders by default" asserts `args: ["-y", "@omilia/mcp-server", "run"]`, but lib/config.js currently emits `github:omilia/mcp`. This package-name/DOC mismatch is owned by Phase 4 (package-name reconciliation). Do NOT touch lib/config.js or that test in this plan. If that single assertion is red on entry, it remains red; the Phase 1 gate is that the golden-SHA test and all other currently-passing tests stay green.
  </acceptance_criteria>
  <done>parseInitOptions no longer throws on missing client; bare init launches the wizard in a TTY; no-TTY without flags exits 1 via buildNoTtyError; fully-flagged-but-missing-client returns 1 via the SUPPORTED_CLIENTS guard; fully-flagged and --print/--write paths return integers unchanged; golden SHA preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests — wizard launch, no-TTY guard, non-interactive, masking, wizard+--write ordering</name>
  <files>test/cli.test.js</files>
  <read_first>
    - test/cli.test.js (the file being extended — reuse createIo; add isTTY to injected io)
    - lib/cli.js (runInit branches from Task 1)
    - lib/wizard.js (buildConfirmationSummary, runWizard contracts)
  </read_first>
  <behavior>
    - A createIo variant exposes input with isTTY and a scripted readable for stdin so tests drive the wizard without a real terminal.
    - Test: bare `init` with isTTY true and scripted answers (client, auth=pat, base-url, token) reaches a confirmation summary on stdout, the summary masks the token (raw token absent), and runInit resolves to 0.
    - Test: bare `init` with isTTY false (no flags) returns 1, stderr contains an actionable message naming --client (WIZ-06), and the call does not hang (resolves synchronously / immediately).
    - Test: fully-flagged `init --client claude-code --auth pat --base-url ... --access-token ... --print` returns 0 with NO prompt text on stdout (CI-safe, WIZ-05) — assert stdout is the config JSON only, no prompt labels.
    - Test (WIZ-04 ordering on the write path): bare `init --write --path <tmpfile>` with isTTY true and scripted answers. Assert (a) the confirmation summary string appears in captured stdout, (b) the target file exists after the call, and (c) ORDERING — the confirmation summary is emitted before the file is created. Verify ordering by asserting the summary substring is present in stdout AND, since runInit writes the summary to stdout before calling finishInit (which performs the write), the resolved exit code is 0 and the file content parses as JSON. (Ordering is structurally guaranteed by Task 1: summary write precedes finishInit; the test asserts both the summary presence and the resulting file to confirm both halves ran.)
    - Test: existing --print/--write tests continue to pass with the new io shape (no isTTY needed where flags are complete).
  </behavior>
  <action>
    Extend test/cli.test.js (node:test, node:assert/strict). Add a helper that builds an io with a scripted stdin Readable carrying newline-delimited answers and input.isTTY set, plus the existing stdout/stderr capture. Add tests covering:
    (1) interactive bare init reaching a masked confirmation summary and resolving 0 (await the possibly-Promise return via Promise.resolve);
    (2) no-TTY bare init returning 1 with stderr naming --client;
    (3) fully-flagged non-interactive --print producing only config JSON on stdout with zero prompt labels;
    (4) the confirmation summary does not contain the raw entered token;
    (5) WIZ-04 wizard + --write ordering: build a scripted-TTY io, run `runCli(["init","--write","--path",outputPath], io)` (outputPath in a mkdtempSync dir) with scripted answers, await via Promise.resolve, then assert the captured stdout CONTAINS the confirmation summary string, existsSync(outputPath) is true, JSON.parse(readFileSync(outputPath)) succeeds, and the resolved exit code is 0 — proving the masked summary was emitted before the file write completed.
    Use await Promise.resolve(runCli(...)) so both sync-integer and async-Promise returns are handled. Do not modify the golden-SHA test. Do not modify the pre-existing Cursor `@omilia/mcp-server` assertion (known Phase 4 issue).
  </action>
  <verify>
    <automated>node --test test/cli.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/cli.test.js` passes including the five new tests; the golden-SHA test and all other currently-passing tests stay green.
    - Behavior: the interactive-flow test asserts the raw entered token string is NOT present anywhere in captured stdout.
    - Behavior: the fully-flagged --print test asserts captured stdout parses as JSON and contains no prompt label substrings (e.g. "Base URL", "Select").
    - Behavior (WIZ-04 write path): the wizard + --write test asserts (a) captured stdout contains the confirmation summary, (b) existsSync(outputPath) === true after the call, and (c) the written file parses as JSON with exit code 0.
    - Note: the pre-existing "builds Cursor config with inline literal placeholders by default" assertion mismatch (`@omilia/mcp-server` vs `github:omilia/mcp`) is a known Phase 4 issue, out of this plan's scope — do not fix or touch it here.
  </acceptance_criteria>
  <done>Integration tests prove WIZ-01 launch, WIZ-04 masked summary (including before the file write), WIZ-05 non-interactive flag path, and WIZ-06 no-TTY guard; the golden-SHA and other currently-passing tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args/env → init | Token/password may arrive via flags (CI) or prompts; neither may be echoed or written to logs |
| init → stdout (confirmation) | Summary printed before any change; secrets must be masked there (WIZ-04) |
| non-TTY environment → init | Missing-input case must fail closed (exit 1) rather than hang on a prompt (WIZ-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Information Disclosure | confirmation summary on stdout | mitigate | runInit uses buildConfirmationSummary (masked); integration test asserts raw token absent from stdout |
| T-01-08 | Denial of Service | no-TTY prompt hang | mitigate | runInit checks io.input.isTTY; no-TTY missing-input path returns 1 immediately via buildNoTtyError, never calls runWizard |
| T-01-09 | Information Disclosure | shell history / CI logs of flags | accept | Operators passing --access-token on the command line accept shell-history exposure; documented; interactive path (no flags) is the masked default |
| T-01-10 | Tampering | regression of --print/--write contract | mitigate | Golden-SHA test and existing write tests run unchanged; flag path returns integer synchronously |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; Node stdlib + Plan 01/02 modules only |
</threat_model>

<verification>
- `node --test` (full suite, all files) passes except the known pre-existing Cursor `@omilia/mcp-server` assertion (Phase 4 scope).
- PAT golden-SHA test passes unchanged.
- Interactive flow reaches masked confirmation; raw token absent from stdout.
- Wizard + --write: confirmation summary emitted before the file is written; file exists after.
- No-TTY bare init returns 1 with actionable stderr; does not hang.
- Fully-flagged --print emits config JSON only (no prompts).
</verification>

<success_criteria>
1. Bare `init` in a TTY launches the wizard, does not error (WIZ-01); parseInitOptions no longer throws on missing client.
2. Confirmation summary shown before any change (including before --write), secrets masked (WIZ-04).
3. Fully-flagged invocation runs non-interactively, zero prompts (WIZ-05).
4. No-TTY + insufficient flags exits 1 with a clear message, no hang (WIZ-06).
5. Existing --print/--write behavior and golden SHA intact.
</success_criteria>

<output>
Create `.planning/phases/01-interactive-wizard/01-03-SUMMARY.md` when done.
</output>
